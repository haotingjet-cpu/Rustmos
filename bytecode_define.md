# My Bytecode 語法定義

### 指令區塊
我的 bytecode 語法應該分成
1. 常數儲存區
2. 指令區塊

應該這樣被區隔
```
.register_num 5

.fdata
    ...

.code
    ...
```
使用 .fdata 與 .code 進行區塊分隔，.fdata 指儲存型別為 f32 的常數， .code 區塊是執行的指令碼，在內部打上所有要被進行的指令

至於 .register_num 指令僅用來定義暫存器個數，受限於 u8 的大小位元數，暫存器個數最高不能超過 255 個

## .fdata 區塊
### 語法
.fdata 區塊為存放浮點數的區塊，內部的語法為
```
.fdata
    a 3.1
    b 6.5
    c 3.1256
```
其中 a, b, c 為為了方便閱讀所定義的變數名稱，由於 .fdata 中所有變數型別皆為 f32 ，因此整體的實現會以 list 的方式去實現，每次的呼叫底層實現也應為 index 的呼叫，由於受限於 u8 的大小位元數，因此 .fdata 能定義的變數數量不可超過 255 個，若有超過得須由請以優化公式 ast (預先計算或包裝成新函數 ...) 的方式來達成目的

### 底層實現
```
.fdata
    a 3.1
    b 6.5
    c 3.1256
```

會被生成為
```RUST
[3.1, 6.5, 3.1256]
```
當然在實現時不可能直接一個陣列走天下，可以使用任何

## .code 區塊語法
.code 區塊內為撰寫所有運算指令的地方，語法為
```
.code
    LOAD R0 a,  0
    LOAD R1 b,  0
    ADD  R0 R0, R1
    MUL  R0 R0, R1
    SUB  R0 R0, R1
    DIV  R0 R0, R1
```
所有指令均由四個字節組成，為

**==指令名 + 目標暫存器 + 參數1 + 參數2==**

至於每個指令都有其參數定義，具體如下
### LOAD
**LOAD** 為將參數載入暫存器的指令，他的可填參數只有 1 個，為載入參數在 .fdata 的命名，後面的 0 為填充符號，以將自節數擴充至 4

假設輸入 **LOAD R0 a, 0** 就是將 R0 暫存器的值設為 .fdada 區塊中的變數名稱，由於 .fdata 區塊由 list 實現，因此變數名 a 字節碼只是一個 index 而已，例如 0x11 為 fdata[17] 的值


### 運算指令
運算指令包含

* ADD
* SUB
* MUL
* DIV

可填參數有兩個: 前暫存器, 後暫存器，例如 **ADD R0 R0, R1** 就是將 R0 和 R1 的值加起來再儲存回 R0，**DIV R0 R0, R1** 就是將 R0 和 R1 的值相除 ( R0 / R1) 再儲存回 R0

#### 指令編號
|指令名|字節碼|
|--|--|
|LOAD|0x00|
|ADD|0x01|
|SUB|0x02|
|MUL|0x03|
|DIV|0x04|

## Example
運算式:
```rust
(0.5 + 4.6) * 6.9 / (6.9 + 5.0)
```
程式:
```
.register_num 3

.fdata
    a 0.5
    b 4.6
    c 6.9
    d 5.0

.code
    LOAD R0 a,  0
    LOAD R1 b,  0
    ADD  R0 R0, R1
    LOAD R1 c,  0
    MUL  R0 R0, R1
    LOAD R1 c,  0
    LOAD R2 d,  0
    ADD  R1 R1, R2
    DIV  R0 R0, R1
```
bytecode:

.fdata:
```rust
[0.5, 4.6, 6.9, 5.0]
```
.code:
```rust
[
    0x00, 0x00, 0x00, 0x00,
    0x00, 0x01, 0x01, 0x00,
    0x01, 0x00, 0x00, 0x01,
    0x00, 0x01, 0x02, 0x00,
    0x03, 0x00, 0x00, 0x01,
    0x00, 0x01, 0x02, 0x00,
    0x00, 0x02, 0x03, 0x00,
    0x01, 0x01, 0x01, 0x02,
    0x04, 0x00, 0x00, 0x01,
]
```

---

運算式:
```rust
(6.0 + 9.2 * 3.0) / 3.0 - 1.0
```
程式:
```
.register_num 2

.fdata
    a 6.0
    b 9.2
    c 3.0
    d 1.0

.code
    LOAD R0 b,  0
    LOAD R1 c,  0
    MUL  R0 R0, R1
    LOAD R1 a,  0
    ADD  R0 R1, R0
    LOAD R1 c,  0
    DIV  R0 R0, R1
    LOAD R1 d,  0
    SUB  R0 R0, R1
```

bytecode:

.fdata
```rust
[6.0, 9.2, 3.0, 1.0]
```

.code 
```rust
[
    0x00, 0x00, 0x01, 0x00,
    0x00, 0x01, 0x02, 0x00,
    0x03, 0x00, 0x00, 0x01,
    0x00, 0x01, 0x00, 0x00,
    0x01, 0x00, 0x01, 0x00,
    0x00, 0x01, 0x02, 0x00,
    0x04, 0x00, 0x00, 0x01,
    0x00, 0x01, 0x03, 0x00,
    0x02, 0x00, 0x00, 0x01,
]
```